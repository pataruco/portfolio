<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h3><%= @projects.where(name:'Glide')[0].name %></h3>

      
    </div>
  </div>
</div>





<% @projects.each do |project| %>
  <ol>
    <li><%= link_to project.name, project_path(project) %></li>
  </ol>
<% end %>

<p><%= @projects.where(name:'Glide')[0].name %></p>


  